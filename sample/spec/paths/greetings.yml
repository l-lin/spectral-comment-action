greetings:
  get:
    tags:
    - greetings
    description: Simple greeting
    operationId: getGreetings
    responses:
      200:
        description: Greeting from the server
        content:
          application/json:
            schema:
              $ref: '../schemas/greetings.yml#/greeting'
            examples:
              sample:
                $ref: '../examples/greetings.yml'

